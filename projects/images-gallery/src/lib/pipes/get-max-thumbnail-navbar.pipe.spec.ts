import { GetMaxThumbnailNavbarPipe } from './get-max-thumbnail-navbar.pipe';

describe('GetMaxThumbnailNavbarPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMaxThumbnailNavbarPipe();
    expect(pipe).toBeTruthy();
  });
});
