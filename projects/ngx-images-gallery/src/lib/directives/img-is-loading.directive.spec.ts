import { ImgIsLoadingDirective } from './img-is-loading.directive';

describe('ImgIsLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgIsLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
